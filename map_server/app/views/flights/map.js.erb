var map;
function initialize() {

  var mapOptions = {
    center: new google.maps.LatLng(38.9639605, -77.379074),
    zoom: 8
  };
  map = new google.maps.Map(document.getElementById("map-canvas"),
    mapOptions);
  }
  //google.maps.event.addDomListener(window, 'load', initialize);
}
